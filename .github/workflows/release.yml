name: Build and publish kernel modules

on:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.name }}
    runs-on: ubuntu-latest
    container: ${{ matrix.image }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: debian-12
            image: debian:12
            package_manager: apt
            headers: linux-headers-amd64
          - name: ubuntu-22.04
            image: ubuntu:22.04
            package_manager: apt
            headers: linux-headers-generic
          - name: ubuntu-24.04
            image: ubuntu:24.04
            package_manager: apt
            headers: linux-headers-generic
          - name: kali-rolling
            image: kalilinux/kali-rolling:latest
            package_manager: apt
            headers: linux-headers-amd64
          - name: fedora-40
            image: fedora:40
            package_manager: dnf
            headers: kernel-devel
          - name: archlinux
            image: archlinux:base
            package_manager: pacman
            headers: linux-headers
          - name: amazonlinux-2023
            image: amazonlinux:2023
            package_manager: dnf
            headers: kernel-devel
    steps:
      - name: Prepare Amazon Linux checkout tools
        if: matrix.name == 'amazonlinux-2023'
        run: dnf -y install tar gzip git
      - uses: actions/checkout@v4
      - name: Build module and CLI
        run: ./scripts/ci-build.sh '${{ matrix.name }}' '${{ matrix.package_manager }}' '${{ matrix.headers }}'
      - uses: actions/upload-artifact@v4
        with:
          name: rayfw-${{ matrix.name }}
          path: dist/*.tar.gz
          if-no-files-found: error

  release:
    name: Publish random release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true
      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          tag="release-$(date -u +%Y%m%d-%H%M%S)-$(openssl rand -hex 4)"
          gh release create "$tag" dist/*.tar.gz --repo "$GITHUB_REPOSITORY" --title "$tag" --generate-notes --target "$GITHUB_SHA"
